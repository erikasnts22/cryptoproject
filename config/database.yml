default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password: postgres
  host: localhost
  port: 5432

development:
  <<: *default
  database: cryptoapp_dev
  
test:
  <<: *default
  database: cryptoapp_test

production:
  <<: *default
  database: cryptoapp__prod